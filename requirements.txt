# --- web ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.5
python-dotenv==1.2.3

# --- auth ---
# passlib removed 2026-09: last released 2020-10-08 (dead upstream), and its
# bcrypt<4.1 pin (reads the bcrypt.__about__ attribute removed in 4.1+) was
# permanently unfixable as a result. Its only job here was CryptContext over
# a single scheme, i.e. no real abstraction being used - see backend/security.py.
bcrypt==5.0.0
python-jose[cryptography]==3.5.0

# --- agents / llm ---
google-adk==2.9.0
litellm==1.100.1        # was pinned at 1.72.0 (>=1.78 needs Python 3.11's
                        # typing.NotRequired). Unblocked 2026-09 by moving the
                        # project to Python 3.13 - see runtime.txt/nixpacks.toml
                        # and notes/09-observability-and-tracing.md.
openai==2.54.0          # litellm==1.100.1 caps openai at <3.0.0; this is that
                        # cap's latest 2.x release, not a deliberate downgrade
                        # from the 3.13.0 this app briefly ran - openai's 2.x
                        # line is still actively released, just not >=3.

# --- rag ---
pinecone==10.0.0

# --- tracing ---
langfuse==4.15.2
openinference-instrumentation-google-adk==0.1.27
openinference-instrumentation-openai==0.1.60
# google-adk caps opentelemetry-api/sdk at <=1.42.1; langfuse alone would pull
# a newer one. Pinned explicitly so a fresh install resolves in one pass
# instead of pip installing 1.44.x first and then conflicting.
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1
opentelemetry-exporter-otlp-proto-http==1.42.1

# --- misc ---
httpx==0.28.1
